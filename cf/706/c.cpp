#include "bits/stdc++.h"

int main() {
  std::ios::sync_with_stdio(0);
  int tc; std::cin >> tc;
  while (tc--) {
    int n, m; std::cin >> n >> m;
    std::vector<std::string> a(n);
    for (int i = 0; i < n; ++i) {
      std::cin >> a[i];
    }

    
  }
}
