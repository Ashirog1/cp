#include "bits/stdc++.h"

int main(int argc, char*argv[]) {
  std::ios::sync_with_stdio(0);

  if (argc > 1) {
    assert(freopen(argv[1], "r", stdin));
  }

  int n; std::cin >> n;
  for (int i = 1; i <= 2 * n; ++i) {
    int u, v; std::cin >> u >> v;
  }
}
