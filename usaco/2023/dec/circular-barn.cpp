#include "bits/stdc++.h"

int main() {
  std::ios::sync_with_stdio(0);

  while (tc--) {
    int n; std::cin >> n;
    std::vector<int> a(n);
    for (int i = 0; i < n; ++i) std::cin >> a[i];
    std::vector<int> required_move(n);
    for (int i = 0; i < n; ++i) {
      if (a[i] % 4 == 0) {
        required_move[i] = 
      }
    }
  }
}
